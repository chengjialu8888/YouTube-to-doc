---
name: youtube-transcript-digest
description: 把 YouTube 视频、播客对谈（Lenny's Podcast / Substack）、公开课等长视频音频的转录稿，整理成一份带置信度审计的中文精译加洞见飞书文档。能力包括：抓取官方字幕或页面 transcript 并存成带元信息的本地 Markdown、识别内容营销性质并对每节标注置信度档位、按对谈自身逻辑线或用户提纲重组内容、用“判断先行—证据跟进—边界收束”的中文文风增强论证、TL;DR 带正文锚点跳转、章节标题写成"主题：一句判断"、顶部和每章配可编辑飞书画板、结尾给"可以搬走的 N 条"、执行内置语言质检去 AI 味、把原文提到的外部文章单开一篇翻译稿。适用于：整理 YouTube 转录稿、播客扒逐字稿、视频转中文文字稿、"帮我翻译并整理这场对谈的主要洞见"、"按这个提纲整理"、Lenny 播客 / YC / Stanford 公开课解读、访谈内容沉淀成飞书文档。
author: jialu
---

# YouTube / 播客转录稿整理

把一场对谈变成一份能直接拿去用的中文文档。

这件事有两层，缺一层就是白干：

**重组**——不逐句翻译，按线索把内容拆开重装，每节讲透一个论点。
**审计**——嘉宾说的话有多少能信？创始人上播客本质是内容营销，哪些是可核实事实、哪些只是自述、哪些跟外部证据打架，必须逐节标出来。只有重组没有审计，产出的就是一篇加长版软文。

## 第一步：先问清楚产出形态

同一个链接有四种完全不同的交付物，不要猜。

| 用户说 | 产出 |
|---|---|
| "获取 transcript" / "扒一下逐字稿" | 本地 `.md`，英文原文带时间戳，到此为止 |
| "翻译全文" / "中文逐字稿" | 本地中英两份 `.md`，中文保留时间戳，按话题加小标题 |
| "整理主要洞见" / 给了一份提纲 | 飞书文档：重组的精译＋洞见＋置信度（**最常见**） |
| "做个总结" | 飞书文档，节奏更短，不逐句精译 |

**用户给了提纲**：正文严格贴着提纲条目走，一条一个 h2，顺序不动。
**用户没给提纲**：按对谈自身的叙事逻辑排，默认序是「起源 → 关键产品决策 → 早期用户 → 架构 → 迭代原则 → 取舍/砍掉的东西 → 真实用例 → GTM → 护城河 → 上手建议」，按实际内容删减。

## 第二步：取稿

不同来源走不同路子，细节和踩坑见 [fetching-transcripts.md](references/fetching-transcripts.md)。

**YouTube**：优先用 `youtube-video-info` skill（它管着登录 cookie）。要一步到位拿标准格式：

```bash
python3 scripts/fetch_youtube_transcript.py <url-or-video-id> -o transcript_<vid>.md
```

沙箱 IP 会被判定为机器人，脚本退出码 3 就是 cookie 过期了，去 `youtube-video-info` 刷新，别反复重试。

**Substack / Lenny's Newsletter**：URL 后加 `?showTranscript=true` 再 `web_fetch`。页面很大，工具会把全文落到 `tool_outputs/*.txt`（一个 JSON），用 Python 取 `result` 字段反转义，再手工定位 transcript 的头尾，把页面壳和底部导航切掉。

**任何已经拿到的原始字幕**（json3 / vtt / srt / 从页面复制的 transcript 面板文本）都可以直接标准化：

```bash
python3 scripts/format_transcript.py raw.vtt -o transcript.md \
  --title "标题 — 逐字稿（Transcript）" \
  --meta "视频链接: ..." --meta "频道: ..." --meta "字幕来源: 官方人工字幕"
```

它会自动识别格式、合并碎片、去掉自动字幕的滚动重复、识别说话人，输出统一的 `**[mm:ss] 【Speaker】** text`。

**逐字稿文件的头部必须写清楚这几行**：标题、播客/频道、嘉宾（带一句身份）、主持人、时长、来源 URL、字幕来源（官方人工 / 自动生成）。字幕是自动生成的要如实写，这决定了引用原话时该多小心。这个文件留在工作区，后面所有引用和时间戳核对都靠它。

## 第三步：置信度审计（写作之前做）

**这一步在动笔之前完成**，因为它会改变每一节的写法。完整方法见 [confidence-audit.md](references/confidence-audit.md)，要点：

1. **先定性这场对谈的利益关系**。主持人和嘉宾什么关系？是不是双赢内容营销（主持人是重度用户、合作方，嘉宾是产品负责人）？这不是黑它，是给读者交代阅读姿势。
2. **找"零提及"**。去社区论坛、官方文档、公开测评、你自己的一手实测里翻这个产品最被骂的三五个问题，然后核对对谈里提没提。**没提的部分比提了的部分信息量更大**。典型模式：工程债大方承认，信任债（计费、隔离、稳定性）装死。
3. **给每一节定档**，三档：
   - **✅ 高可信** — 被一手实测、官方文档或多个独立来源印证。
   - **💡 新信息且合理** — 只有当事人视角、无从外部验证，但细节自洽、与公开时间线吻合。当产品史素材用可以，别当可核实事实引用。
   - **❗ 营销口径** — 与实测证据直接打架，或是无法证伪的自我评价。必须打红旗，写清楚打架在哪。
4. 审计结论写进「来源与整理方式」里的置信度框架 callout，档位写进每节末尾的 callout。

审计不是可选的严谨性装饰。用户拿这份文档去做判断，标错档比漏一条洞见的代价大得多。

## 第四步：重组和精译

写作与语言优化规范见 [translation-style.md](references/translation-style.md)，完整 XML 模板见 [lark-doc-blueprint.md](references/lark-doc-blueprint.md)。语言规范已经完整收录在 Skill 中，不依赖作者的私人文档、工作区文件或外部 prompt，换到其他用户和空间也能按同一标准执行。

文档骨架，从上到下：

1. **一张图看完** — 加粗一行引题 + 一张总览画板，全篇最先出现。
2. **主要判断** — 💡 黄色 callout，5 条，每条「加粗小标题：一句话」。只看这块也能带走东西。
3. **来源与整理方式** — 对谈来源和链接、嘉宾背景、时长、一句话概括这场对谈在讲什么，然后是**置信度框架 callout**（第三步的产出）。
4. **TL;DR** — 有序列表，8–12 条。每条「加粗判断 + 三五句展开」，关键句挂**正文锚点链接**（`<文档URL>#<block_id>`）让读者一跳到位。这块取代花哨的卡片，信息密度才是重点。
5. **正文各章** — 一章一个 h2，标题格式固定为 **`主题：一句判断`**（"起源故事：一个月，一个山洞"、"GTM：90% 与 100% 是两个物种"、"护城河：被发现的，不是规划出来的"）。h2 下第一个元素是本章画板，然后 h3 拆小论点，章末一个置信度 callout。
6. **Summary：可以搬走的 N 条** — 有序列表，可嵌套子条。开头明说"以下是我根据 XX 分享内容的推断和总结"，每条末尾标 `〔对应第 N 节〕` 回指正文。

**章节标题的判断句是这份文档最值钱的部分**。"GTM 策略"是标签，"90% 与 100% 是两个物种"是判断。只写标签等于没读懂。

**每个 h3 的写法**：先用中文把嘉宾的意思讲透（该合并合并、该补背景补背景），关键处配一段原话。原话格式是英文原文加中文翻译加时间戳：

> "Truly throwing a no-look pass to a colleague and being like you got this." —— 真正像给同事传一个不看人的传球：这事你能行。[01:03:05]

**事实和推断必须分开**。嘉宾说的和你推的不能混在一句里，自己的推断用 `〔推断〕` 前缀。主持人的补充观察要标明是主持人说的。

用户之后说"就这条单独展开一个小节"，做法是把它提成独立 h2，补足证据和引用，插到相关章节后面，同步更新 TL;DR 和 Summary 的回指编号。

## 第五步：配图

用 `beautiful-feishu-whiteboard` skill。**顶部一张总览图 + 每个 h2 一张 + Summary 一张**，画板放在章节标题的正下方。

- **全篇一个 palette**，不要每张换风格。
- 生成的是**可编辑飞书画板**，不是截图。
- **必须线上复核**：`whiteboard +export --output-type preview` 拉预览图逐张看。常见问题是无文字的装饰色块（删掉就好）和标题条缺描边（补 `stroke` + `stroke-width`）。改完用 `block_replace` 换掉文档里那个 whiteboard block。

## 第六步：语言优化与去 AI 味

按 [translation-style.md](references/translation-style.md) 的「判断先行—证据跟进—边界收束」规范逐段改写，再跑文末的自检清单。这套规则完整内置，不要求外部用户安装额外 Skill。

如果当前环境碰巧提供 Humanizer 类能力，可以把它作为第二遍辅助扫描；最终判断仍以内置规范为准，不能把外部依赖当成完成条件。高频问题：开头铺背景不下判断、抽象名词成串、因果链断裂、结论超出证据、破折号泛滥、机械排比、段末重复总结。

## 第七步：延伸材料

对谈里被郑重推荐的外部文章（清单、blog、论文），单开一篇全文翻译文档，别塞进主文档。卷首写一段推荐语：谁在哪儿推荐的、他的论点是什么、这东西和主文档什么关系。原文的所有 source 链接一个不漏地保留。

## 交付

一次给全：飞书文档 URL、本地逐字稿文件、延伸翻译文档（如果有）。改动过的说清楚改了什么。

## 常见翻车

- **只转述不审计**。把创始人的自述当事实抄一遍，产出就是软文。
- **章节标题只写标签**。"护城河"和"护城河：被发现的，不是规划出来的"，差距就是有没有读懂。
- **逐句直译**。用户要的是重组，机械对译的稿子他不会用第二遍。
- **提纲之外的内容被丢掉**。宁可收进最后一章，也不要静悄悄扔掉。
- **原话没有时间戳、没有英文原文**。引用要可回查、可核对翻译。
- **推断混进事实**。这是最伤信任的一种错。
- **TL;DR 没挂锚点**。十条判断读者想深挖却找不到正文位置，等于只做了一半。
- **写完就交**。没完成语言质检、没线上看图，等于没做完。
